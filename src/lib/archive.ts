/**
 * Posts per archive page. The archive used to render every post on one URL,
 * which grows without bound; paginating keeps each page a fixed size no matter
 * how many posts the blog accumulates.
 */
export const ARCHIVE_PAGE_SIZE = 24;
